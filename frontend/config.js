// OpportunityFinder deployment override.
// Leave this as null for same-origin deployments.
// Set it to a full API base URL before deploying the frontend separately.
window.OPPORTUNITYFINDER_API_BASE = window.OPPORTUNITYFINDER_API_BASE || null;